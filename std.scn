; define a function to map across elements
(define (map fn lst)
  (if (null? lst)
    '()
    (cons (fn (car lst)) (map fn (cdr lst)))))

; unary predicates
(define (null? lst)
  (eq? lst '()))

(define (zero? val)
  (eq? val 0))
