(include "std.scn")

; this is a comment
(define (! n)
  (if (zero? n)
    1
    (* n (! (- n 1)))))


[ Mapping the factorial function across 2 4 and 5]
(map ! '(2 4 5))

(define crashIf (fexpr (pred crasher)
  (if pred
    crasher
    #t)))


(crashIf #f (/ 1 0))


(define (caar lst)
  (car (car pairs)))


(eval (+ 12))

